# Heroku Environment Variables Example
# Copy these and set them using: heroku config:set KEY=VALUE

# Node Environment (Required)
NODE_ENV=production

# Sumo Logic HTTP Source URL (Required for log monitoring)
# Get this from: Manage Data > Collection > Add HTTP Source
SUMO_LOGIC_URL=https://collectors.sumologic.com/receiver/v1/http/YOUR_TOKEN_HERE

# Firebase Configuration (If using Firebase)
FIREBASE_API_KEY=your_firebase_api_key_here
FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_STORAGE_BUCKET=your_project.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your_sender_id
FIREBASE_APP_ID=your_app_id

# Database (If using PostgreSQL - automatically set by Heroku)
# DATABASE_URL=postgres://username:password@host:port/database

# Redis (If using Redis - automatically set by Heroku)
# REDIS_URL=redis://username:password@host:port

# Custom Variables (Add your own as needed)
# API_SECRET=your_secret_key
# JWT_SECRET=your_jwt_secret

# How to set these on Heroku:
# heroku config:set NODE_ENV=production
# heroku config:set SUMO_LOGIC_URL=your_url
# heroku config:set FIREBASE_API_KEY=your_key
# etc.

# View all config vars:
# heroku config

# View a specific var:
# heroku config:get NODE_ENV

